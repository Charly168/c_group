1. 如果采样了* , 则必须要分配new 空间， 同时在西沟函数里里面delete
2. 如果采样了拷贝构造函数，先删除指针，再给空指针，再创new.最后的delete 统一在
西沟函数
3. 在默认构造函数中最好先赋值空指针,且新给一个new空间    str = new char[4];
    strcpy(str,"C++");
4. 成员operator函数包含=, 但是不包含其他的比如<< >> ,> < + -. 如果遇到了，要么在
令为全局函数，不能在类里面的定义，friend 

